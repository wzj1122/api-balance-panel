# 变更日志（CHANGELOG）

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)；格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.3.1] - 2026-09-22

### 修复（真实日志定位：小米 MiMo / 商汤「登录成功却一直显示等待登录」）
用本机 `main.log` + 登录分区的真实 Cookie 数据库交叉验证，定位到两个根因：

- **根因 1：登录窗口与静默续期用的不是同一个分区**（决定性证据）
  - 登录窗口原来按 `new URL(登录页).hostname` 建分区，小米登录页在 `account.xiaomi.com` ⇒ 登录态落在 `persist:login-account.xiaomi.com`；
    而静默续期用的是 `persist:login-platform.xiaomimimo.com`。
  - 实测两个分区的 Cookie：登录分区里有一个 **2026-09-21 01:38:54 新建的 passToken**（就是用户那次成功登录），
    而续期分区里的 passToken 是 **2026-09-09** 的老货（12 天没更新过）——两份会话互不相通，续期永远拿不到能用的会话。
  - 商汤同理：`persist:login-platform.sensenova.cn` 分区里**只有百度统计 Cookie**，没有任何会话 Cookie，续期脚本换不回 token。
  - 修法：新增唯一口径 `LOGIN_RULES`（shared/constants.ts），登录与续期都从它取分区 host；登录窗口显式指定 `partitionHost`，
    采集续期材料也改从该分区读——两边再也不可能漂移。
- **根因 2：登录流程「关了窗就当成功」，从不复验保存下来的凭据**
  - 以前只校验"窗口里抓到的凭据能过接口"，然后就关窗、静默保存；一旦保存/读取链路上出问题（如上面那个分区不对），
    用户看到的就是"登录成功、窗口自动关闭、卡片却一直等待登录"，而且不知道该再点一次。
  - 修法：新增**保存后复验**（`LoginOptions.verify`）——先真的写进账号，再用平台适配器实跑一次查询；
    只有真能查到数才算登录成功。复验不过就不关窗、如实报错（界面也会把失败原因显示出来）。
- **其它健壮性修复**
  - `loginToSite` 的 Promise 增加硬超时兜底（10 分钟）与关窗处理异常捕获：以前某一步卡住会让 Promise 永久挂起，界面永远停在"等待登录"；
  - 「复用既有会话」不再只看 Cookie 名字：小米分区里那个 09-09 的旧 passToken 名字是对的、其实早已失效，
    现在配了校验规则就用真实接口验一次，不通过直接走正常登录（省掉一次 20 秒的白等）；
  - 登录失败时界面显示 `✗ 原因`，不再只留一行容易被忽略的小字。

### 说明
- 本次修改**不动**任何账号数据与快照；登录分区名变化只影响下一次登录/续期的读写位置（老的 `login-account.xiaomi.com` 分区会被弃用）。

## [1.3.0] - 2026-09-22

### 新增（积分与金额可以放在同一把尺子上了）
- **「设置 → 成本折算」：积分折算率**（多少积分算 1 元），默认 **111111**，即 **1 元 ≈ 11.11 万积分 / 100 万积分 ≈ 9 元**，可一键恢复默认，填 0 = 不折算。
  - 默认值来源（按 DeepSeek flash **高峰时段**价反推）：输入缓存命中 0.04、未命中 2、输出 8 元/百万 Token；某窗口输入 8.1M（缓存命中 7M）+ 输出 160.6K ≈ **3.76 元**，折算 ≈ 0.45 元/百万 Token，取 ~0.9 元/百万 Token 作保守上限 ⇒ 100 万积分 ≈ 9 元。
  - 折算只影响展示层（估算列与对比条排序），**不改动任何原始快照与统计数字**。
- **平台用量页新增「折合人民币（估算）」口径**：
  - 顶部新增「折合人民币（估算）」信息卡，给出**唯一一个跨单位可比**的总额；「消耗最多平台」也改为按折算值判定并标注折算金额；
  - 对比条形图**按时人民币估算值排序定宽**，每行同时显示原值（如 `358.83 万 积分`）与折算值（`≈ 32.30 元`）；
  - 明细表新增「折合人民币」列，CSV 导出同步新增该列，并按折算值顺序导出。

### 修复
- **分享卡片「其它单位」与「较上期」两行文字重叠**：原来三行写死 `y = 370 / 408 / 424`，24px 与 30px 字号的字高都超过 16px 的行距，后两行直接压在一起。
  现在**每行独占一行、按行高（42px）递推 baseline**，「其它单位」与「环比」各占一行；数据格与「消耗构成」标题跟着实际行数下移，多一行也不会压到下面的内容。
- **「其它单位」文本过长会被画布裁掉**：新增 `fitFont`（按实测宽度逐级缩字号）+ `wrapText`（按画布宽度自动换行），单位再多也完整显示。
- **平台用量「数字对比」失真（积分 vs 金额被当成同一量级）**：对比条原来用「原值 / 最大值」定宽，积分数值天然比元大几个数量级 ⇒ 积分条永远满格、金额条几乎看不见，等于没在比。现改为统一按折合人民币排序定宽。

### 改进（黄颜色不再是"没什么特殊地方"）
- 「每日使用状况」表格新增**颜色图例**：黄色 = 停电期间的消耗（软件未运行、无法按天归属）、橙色「充值」= 当天余额反而增加（当日消耗算不出来）、预计耗尽三档（红 < 3 天 / 黄 < 14 天 / 绿 充足）。
- 逐日单元格与日期列表头都加了 `title` 悬浮说明：具体到「消耗 X · 停机期间另有 Y，已计入合计」；充值日另加下划虚线，与「停机期间」的纯黄色区分开。

### 文档
- `README.md`、`docs/使用说明.md` 与内置说明同步：平台用量的折算口径、成本折算设置项、表格颜色图例。

### 发布
- GitHub Release **v1.3.0** 已发布，附件：`API.Setup.1.3.0.exe`（Windows x64 NSIS 安装包，129.8 MB）。

## [1.2.1] - 2026-09-21

### 修复（用真实数据定位的三个统计口径问题）
- **AI 使用报告只显示"积分"、余额账号完全看不到**：
  - 报告只挑"数值最大的单位"当主单位（820 积分 > 31 CNY ⇒ 选了积分），平台构成/最多账号/时段分布/充值记录全被过滤到只剩积分；现在**每个单位各算一套完整明细**，顶部给出全部单位的数字，点单位标签即可切换下方明细（不同单位不能相加，所以分开列）；
  - 顺带修掉一个真 bug：`total` 读错了字段（`totalByUnit[0].value` 写成了不存在的属性）⇒ **顶部大数字一直是 `undefined`**，现在正常显示。
- **报告与「每日使用状况」口径不一致（同一个月 303.22 vs 64.50）**：报告原来用另一套"相邻快照差分"算法，会把跨期暴跌算成单次消耗，还会把"读取闪断"累加成消耗（实测阿里云 9/16 17:50 读到 7.78、17:55 又回到 215.75，被记成 207.97 的消耗）。
  现在报告**直接复用每日统计的逐日账目**（含跨期剥离与闪断过滤），两个页面数字一致；实测本月 CNY 从虚高的 303.22 修正为 **40.99**，与日报完全一致。
- **`used` 字段恒定导致整天消耗被算成 0**：部分适配器会填一个几乎不变的 `used`（实测阿里云长期恒为 92.03），旧逻辑只要有值就按"用量型"算 ⇒ 当天消耗 ≈ 0、真实下降被忽略。
  现在要求「余额几乎不动 **且** `used` 确实在增长」才算用量型，否则按余额差值计算。
- **读取闪断（瞬时错值）不再计入消耗**：新增闪断识别——某一跳跌幅 ≥ 当日波动一半、且几分钟内回到接近跳前水平 ⇒ 判定为瞬时错误读数，不计入消耗；真实消耗（跌了不回来）不受影响（实测 deepseek 9/10 的 7.68 真实消耗保留，阿里云的 207.97 假消耗被剔除）。
- **跨零点那次刷新不再被误判为"停机"**：停机判定阈值从「≥30 分钟」提高到「≥2 小时」（并保留刷新间隔×3 的动态下限），避免每天被切掉半天消耗。

### 新增（数据校正增强）
- **「只改这天」**：把某天的余额整体修正到目标值，**前后日期都不受影响**（平移锚点取当天第一条快照，不跨零点）；采用"在应用后的状态上迭代校准 + 候选择优"，无法精确命中时会**如实告知**（例如该天存在闪断）。
- **「忽略这天」**：把某天的异常读数整段从统计里剔除（该天显示为空），比"改成一个数字"更彻底，适合"读到明显错的值"这类情况；同样可随时撤销。
- 校正账本 UI 显示是否"仅当天"，并区分「整体平移 / 设为指定值 / 忽略该段」。

### 文档
- `docs/使用说明.md` 与内置说明同步：报告的"分单位"口径、校正页新增的两种操作。

## [1.2.0] - 2026-09-21

### 新增
- **数据校正（新页面 + 账本机制）**：用于修正明显异常的历史数据（例如网络抖动导致某次刷新读到的余额不对）。
  - **① 改某天某账号的用量**：在「数据校正」页直接填实际值（不受近 7/30 天窗口限制，也能看到每天的余额）；
  - **② 回退到某次状态**：选一条历史快照（或直接填一个准确余额），该时刻起整体平移；
  - **③ 校正账本**：每条校正都能看到生效时间、数值、影响的快照条数、备注，并可**单条撤销**或**一键还原全部**；
  - **④ 三处入口**：侧边栏新增「数据校正」页（主入口）；「每日使用状况」表格里每个账号旁有「校正」直达链接；卡片上出现「已手动校正」标记时，点它跳到校正页；
  - **⑤ 卡片实时数字同步**：校正也套用在实时查询结果上，刷新后不会又变回平台的错值；卡片备注会写明「已手动校正 ±X」。
  - 机制要点：校正记录存在数据目录的 `corrections.json`（只增不删、可撤销），**原始 snapshots.json 一行不改**；校正在「快照读取的唯一出口」统一套用，因此每日使用、平台用量、使用报告、日历、趋势图、耗尽预估、预算判断全部自动跟着对（日均与耗尽预估按校正后的数据重算）。
  - 一致性设计：只提供「从某一时刻起整体平移 / 设为指定值」两种安全操作 —— 相邻快照的**差值**决定每天消耗，整体平移不改变差值，所以**之后每天的消耗依旧准确**（这也是"之后一直有消耗也要同时减去"的实现方式）。

### 修复
- **跨期（停机）判定阈值过小，导致每日消耗被误剥离**：原判定是「与上一条快照间隔 > 刷新间隔 × 3 且 ≥ 30 分钟」就算停机。跨零点那次刷新（最后一条 23:5x、次日第一条 00:0x 前后）或一次 1 小时左右的空档都会被误判为停机，结果是**当天消耗被整段算成 0/半天**、报表数字明显偏小。
  现改为：**硬下限 2 小时**，并保留「刷新间隔 × 3」作为动态下限（刷新间隔调大时同步放宽）。真正的停机（关软件几小时/几天）仍能正确识别。
  实测（合成 4 天数据，每天 2 条快照）：修复前逐日消耗为 `[1.5, 1.5, 0.75, 1.5]`（错误），修复后为 `[1.5, 3, 3, 3]`（正确）。

### 文档
- `docs/使用说明.md` 与内置说明同步「数据校正」用法与停机判定新口径。

## [1.1.8] - 2026-09-21

### 变更
- **「合计」口径改为包含停机区间**（按用户要求）：停机期间（软件未运行）的余额下降不再排除在合计之外。
  - 每日使用状况：新增「合计（含停机）」列（账号行 + 单位合计行），= 逐日之和 + 停机期间；「停机期间」列保留为说明性明细；
  - 平台用量：「合计」列改为含停机口径；
  - 使用日历：每月消耗合计同步改为「该月逐日之和 + 该月停机期间」；
  - 当日明细弹窗：新增「合计」列（用量 + 停机期间）；
  - CSV 导出（每日使用 / 平台用量）新增「合计(含停机)」与「逐日合计」两列，便于对账。
  - **保持不变**：日均（7日均）、耗尽预估、建议阈值、每日预算判断仍**不含**停机期间（避免"关机几天把日均拉高"造成误判）；如需一并计入可再调整。
  - 实测（用户真实快照，近 30 天）：CNY 逐日合计 60.9456 + 停机期间 2.05 = 62.9956；两个含停机的账号相加校验一致。

## [1.1.7] - 2026-09-21

### 新增（延长登录有效期：A + B + C 三件套）
- **登录窗口不再清空既有会话，优先「静默重登」**：以前每次登录都会把登录分区里的 Cookie 一起清掉，等于把"平台那边还有效的会话"主动扔掉——这是需要反复重登的主要原因。现在改为：先复用分区里的会话打开页面，平台若认这个会话会自动换出新 token，窗口自动关闭、**不需要输密码**；20 秒内没成功才清空凭据，进入正常登录流程。
- **后台会话保活（面板隐藏时同样运行）**：新增 `src/main/renewal.ts` + 主进程定时任务，每 20 分钟检查一次登录型账号（商汤／小米 MiMo），凭据距过期不足 60 分钟或已过期时，用保存的会话在**隐藏窗口**里静默换新 token；查询前若发现凭据临期（<15 分钟）也会先续期再查，避免"正好卡在过期那一刻"报错。
- **卡片新增「续期登录」按钮**：一键静默续期，成功后立即刷新该卡片并提示新凭据有效期；会话真的失效时，**自动打开登录窗口**（不用再去「编辑」里找入口），登录成功即自动写回凭据与续期材料。
- 账号新增两个字段：`session_enc`（会话材料密文，含会话 Cookie）、`token_expires_at`（凭据到期时间），二者都用与密钥相同的 safeStorage 加密落盘，明文不落盘、不进日志。

### 实测结论（也写进文档，避免以后误解）
- 商汤：`access_token`/`id_token` 是 **180 分钟** JWT，**没有 refresh token**；`oauth2_authentication_session` 会**滑动续期**（观测到它从"会话级"变成"剩 3 小时"，并与 token 同步）。
- 小米 MiMo：`api-platform_serviceToken` 是会话级 HttpOnly Cookie，靠调用余额接口本身续期；上层 `account.xiaomi.com` 的 `passToken` 约 30 天且同样顺延。
- **把浏览器里的会话 Cookie 复制到独立 Electron 分区是无效的**（实测控制台接口直接 401），所以"跨程序搬运登录态"这条路没有采用；续期只依赖**面板自己那份**持久化会话（与浏览器同源的正常使用方式一致）。

## [1.1.6] - 2026-09-20

### 修复
- **商汤日日新「登录成功、窗口自动关闭，却仍提示重新登录 / 要求输入 key」**。定位到登录窗口（`src/main/browser/index.ts`）四个问题，全部修掉：
  1. **只清 Cookie、没清 localStorage**（主因）：登录分区里残留着**上一次的过期 `access_token`**，一开窗就被当作"已登录"拿去校验，于是窗口"假通过"自动关闭，账号里存下的是过期登录态 → 之后刷新必然提示重新登录。现在清空范围改为 `cookies + localstorage`。
  2. **关窗竞态**：校验通过后是先关窗、再由 `closed` 事件重新读凭据；若读取正好落在页面销毁瞬间就会读到空，直接报"没有读到登录态"。现在校验通过先**落定凭据**再关窗，`closed` 只用已落定的值。
  3. **卡住时不会自愈**：新增主动重载兜底——检测到"人已经在控制台、页面也加载完了，却读不到登录态"时，自动 reload 一次控制台，让平台用会话 Cookie 换取并写回 `access_token`（实测商汤就是这样工作的，不会让用户重新登录、也不会顶掉浏览器里的登录态）。
  4. **登录态来源单一**：除 `localStorage.access_token` 外，新增**只读探针**（注入页面，记录页面自身请求里的 `Authorization: Bearer`）作为兜底来源；顺带修掉探针首次实现里的正则转义 bug（模板字符串里 `\\s` 被写成字面量导致永不匹配）。

### 变更
- 登录排障可见化：等待登录期间每 5 秒记录一次「当前页面 URL + 还没读到登录态的原因」，超时/关窗时把**页面上像登录态的键名与长度**写进日志；登录窗口标题在超时后提示"请在窗口里完成登录，进入控制台后会自动关闭"。
- 阿里云代金券读取恢复正常（复核日志：`代金券：接口返回 7 张，可用 1 张，剩余合计 207.97`），1.1.5 新增的诊断信息保留，便于以后再出现时一眼定位。

## [1.1.5] - 2026-09-20

### 新增
- **新增平台：商汤 日日新（SenseNova）· Token Plan 双积分池**。按官方控制台口径展示三层额度：
  - **5 小时窗口剩余**（卡片主数值 / 进度条，窗口到期自动重置）+ 重置时间；
  - **周额度剩余（7 天）**（周总量与重置时间）；
  - **Flash-Lite 积分池**（Flash-Lite 系列专属池，与通用池分开计量）。
  - 鉴权方式：额度接口只认登录态（Bearer token，存在 localStorage），**不认 API Key**（实测 Cookie 与 API Key 均返回 401）；因此复用内置浏览器登录：点「登录并获取」自动打开 SenseNova 控制台，登录完成后自动检测并关窗，抓取登录态并即时用真实接口校验。登录态约 3 小时过期，过期后卡片提示重新登录。
  - 接口：`GET /lite/console/v1/tokenplan/pool-usage`（字段为字符串数值，已统一解析）。
  - 概览页新增分组「套餐 / 额度」（小米 MiMo 套餐 + 商汤日日新），并加了兜底分组：以后新增平台即使忘了登记分组，也不会出现「账号存在但看不到卡片」。
- 适配器正式测试脚本 `scripts/verify-adapters.mjs` 新增日日新用例（5h/7d/专属池解析、登录态过期、无积分池、缺凭据），并修复该脚本一个既有缺陷：适配器链路会经 `logger → paths → electron` 把 electron 包打进 ESM，导致 `Dynamic require of "child_process" is not supported` 直接崩，现改用 electron 桩，脚本可正常跑（130 项全通过）。

### 修复
- **阿里云代金券「读不到」时不再静默**：以前 `QueryCashCoupons` 失败被静默忽略，用户只看到金额少了一块却说不出原因。现在：
  - 卡片备注会写明「代金券读取失败：<原因>」或「代金券接口返回 N 张但无可用券（状态：…）」；
  - 运行日志记录返回张数 / 可用张数 / 剩余合计；日志级别切到 debug 还会记录每张券的状态、面值、余额、到期时间（券号只记前 6 位）；
  - 券状态判定放宽：只排除明确的过期/已用/冻结状态，避免平台改状态字面量后整批券被判定为不可用。

### 文档
- README、`docs/使用说明.md`、内置使用说明新增「商汤 日日新」接入步骤与额度口径说明；阿里云章节新增「代金券读不到怎么排查」对照表。

## [1.1.4] - 2026-09-14

### 修复
- **「使用报告」页区块间距**：「消耗构成 / 时段分布」下方那两个独立卡片（这段时间的你、疑似充值记录）与底部说明行原本**没有任何间距**，几个方框贴在一起（`.panel` 只有卡片样式、缺下边距，上一行的间距全靠 `.two-col` 的 `margin-top` 撑着）。
  - 竖向间距统一改为 `margin-bottom: var(--gap)`：顶部汇总卡、两列容器、独立面板、底部说明行一律 16px；
  - 两列容器**内部**的面板排除在外（间距交给 grid 的 gap），避免出现 32px 的双倍间距；
  - 实测：四块之间全部为 16px；「每日使用状况」「平台用量」两页复查无同类问题。

## [1.1.3] - 2026-09-14

### 修复
- **开机自启不生效**：重写 `src/main/autostart.ts`，写入系统启动项后**回读注册表校验**，不再"假装成功"：
  - 统一登录项名称：Electron 在 Windows 上写入用的是 `productName`（API 余额面板）、读回用的是 `name`（api-balance-panel），名字对不上导致状态永远读不正确；现在读写都用同一个名字；
  - 写入路径：先走官方 `app.setLoginItemSettings()`，写完后用 `reg query` 回读 Run 键校验；没写进去就自动用 `reg.exe` 直写当前用户 Run 键兜底（无需管理员权限），失败的真正原因会写进日志；
  - 清掉「任务管理器 → 启动」遗留的禁用标记（`StartupApproved\Run`）：Windows 只要看到这条记录就会拦住启动项，之前一旦被禁用就再也开不起来；
  - 关闭自启时同时删除 Run 键与禁用标记，不留残留项；
  - 启动时校准一次：配置里开着但系统启动项被清理掉（换机 / 被优化软件删掉）会自动补写回去；
  - 开机自启启动时**不再弹出主窗口**，直接常驻托盘后台刷新（`--open-at-login`），且这种启动不会被「窗口隐藏时暂停刷新」停掉，开机后余额照常更新。

### 变更
- **「设置 → 显示 → 主题」与「主题外观」页联动**：
  - 深色 / 浅色 = 直接切换当前主题（即主题外观页里的经典深色 / 经典浅色），两边是同一个设置，点一下**立即生效并保存**，不需要再点「保存设置」；
  - 跟随系统 = 按 Windows 深浅色自动在**经典深色 / 经典浅色**之间切换（系统换色时实时跟着变），并在启动时按系统当前状态选好；
  - 跟随系统时，主题对应的内置背景也按解析后的深浅色自动匹配（以前会去找不存在的 `system.png`）；
  - 在「主题外观」页选了设计师主题后，设置页的主题按钮会同步取消高亮，反之亦然。

### 文档
- 「设置 → 系统 → 开机自启」下方新增**真实状态提示**（已写入 / 未写入 / 被任务管理器禁用 / 开发模式跳过），状态来自注册表回读而不是配置里的期望值；开关点一下立即写入启动项，无需再点「保存设置」。
- README、`docs/使用说明.md`、内置使用说明同步说明上述行为。

## [1.1.2] - 2026-09-10

### 文档
- 更新 GitHub Release（v1.1.0）说明，补充**授权与版权**告知（原创作品、需授权范围、联系邮箱）。
- README 与 `docs/` 中的安装包文件名改为通用表述（不再写死具体版本号），并说明 v1.1.1 / v1.1.2 为授权与文档更新、未单独发布安装包。

## [1.1.1] - 2026-09-10

### 变更（授权模式）
- **授权模式由 MIT 变更为「保留所有权利」的专有许可**：本项目为作者原创作品，著作权归作者所有；
  - 允许：个人学习、研究与自用（非商业目的）；
  - 需事先取得作者书面许可：修改源代码、二次开发、衍生作品、复制再分发、任何商业用途、移除署名；
  - 授权申请：18293592881@163.com。
- `LICENSE` 全文替换为专有许可条款；`package.json` 的 `license` 字段改为 `SEE LICENSE IN LICENSE`、`author` 改为作者署名；README 新增「版权与授权」章节并更新许可徽章。
- 说明：v1.0.x / v1.1.0 等历史版本依当时 MIT 许可发布，其授权不变；自本版本起适用专有条款。

## [1.1.0] - 2026-09-10

### 新增
- **停机期间消耗单独标记**：彻底退出软件一段时间后再打开，期间产生的余额下降（无法按天归属）不再堆到"重新打开的那一天"，而是单独统计：
  - 每日使用状况表格新增「停机期间」列（账号行 + 单位合计行），并在页面顶部给出提示条；
  - 近 30 天日历上给对应日子加 **⏸** 标记，当日弹窗明细显示该日的停机期间消耗；
  - 平台用量页同步新增「停机期间」列，CSV 导出也包含该列；
  - 该部分**不计入每日均值、耗尽预估、建议阈值与每日预算判断**（顺带修掉了"停机后重启误报预算超支"的问题）。
  - 判定口径：与上一条快照间隔超过「刷新间隔 × 3」且至少 30 分钟即视为停机（关窗缩到托盘不算，仍会持续刷新）。
- 托盘 tooltip 仍显示最低余额账号/失败数量。

### 变更
- **取消托盘图标的状态变色**：托盘图标固定为软件主图标，不再在"查询失败或低余额"时染红（告警信息仍可通过 tooltip、卡片与系统通知获知）。

### 文档
- README、`docs/使用说明.md`、内置使用说明同步补充"停机期间消耗"的口径说明。

## [1.0.1] - 2026-09-10

### 变更
- **托盘图标统一为软件主图标**：不再使用单独的托盘小图标，正常态显示软件主图标（`icon.png`）；有查询失败或低余额时，主图标染为红色作为告警（保留原有告警语义）。
- **后台运行时可通过快捷方式唤起主界面**：程序已在后台（窗口隐藏到托盘或最小化）时，再次点击桌面/开始菜单快捷方式或重复启动，会把已有主窗口唤到前台，而不再只是聚焦不可见窗口。
- 单实例约束显式化：全程只允许一个实例，重复启动一律复用已有窗口，不产生多个界面。
- 移除冗余的 `focusMainWindow()`（其行为不完整，已被 `showMainWindow()` 取代）。

### 文档
- 新增本 CHANGELOG。
- README、`docs/使用说明.md`、内置使用说明同步为 **安装包（Setup exe）** 版本，并更新版本号引用为 1.0.1。

## [1.0.0] - 2026-09-10

### 新增（首次发布）
- 余额卡片墙：DeepSeek / 硅基流动 / 小米 MiMo（余额 + 套餐）/ MiniMax / 智谱 / 阿里云百炼 / New API 中转站 / 自定义接口
- 自动定时刷新、低余额提醒、余额耗尽预估、每日预算与告警
- 每日使用状况（日历热力图）、平台用量统计、AI 使用报告与分享卡片、CSV 导出
- 接口监控：自填 Key + 调用地址，按间隔真实调用并统计可用率 / 延迟 / 连续失败
- Key 管理：分类标签、回收站、批量操作、导出 Markdown/JSON/cURL
- 14 套主题 + 内置背景 + 玻璃拟态效果、演示模式、系统托盘常驻、开机自启、备份恢复
- 密钥本机加密（Windows DPAPI），渲染进程不接触明文，不上传任何数据

### 修复
- 概览页「删除账号」失效（IPC 入参对象 / 裸字符串不匹配）
- 重启后账号分类标签与回收站状态丢失（配置加载未保留 `tags` / `deleted_at`）
- 卡片「余额耗尽档位」永远显示正常（百分比口径错误）
- 接口监控展开历史后点「检查」会收起面板（toggle 语义冲突）

### 清理
- 删除已被「接口监控」取代的探测模块死代码（`probe.ts`、`buildAvailability` 及相关类型/设置项）
- 删除无调用者的导出与死 CSS，抽取重复的 CSV 导出逻辑与表单字段定义
- 托盘菜单精简为「显示主面板 / 退出」两项
