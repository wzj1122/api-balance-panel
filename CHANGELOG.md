# 变更日志（CHANGELOG）

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)；格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.1.5] - 2026-09-20

### 新增
- **新增平台：商汤 日日新（SenseNova）· Token Plan 双积分池**。按官方控制台口径展示三层额度：
  - **5 小时窗口剩余**（卡片主数值 / 进度条，窗口到期自动重置）+ 重置时间；
  - **周额度剩余（7 天）**（周总量与重置时间）；
  - **Flash-Lite 积分池**（Flash-Lite 系列专属池，与通用池分开计量）。
  - 鉴权方式：额度接口只认登录态（Bearer token，存在 localStorage），**不认 API Key**（实测 Cookie 与 API Key 均返回 401）；因此复用内置浏览器登录：点「登录并获取」自动打开 SenseNova 控制台，登录完成后自动检测并关窗，抓取登录态并即时用真实接口校验。登录态约 3 小时过期，过期后卡片提示重新登录。
  - 接口：`GET /lite/console/v1/tokenplan/pool-usage`（字段为字符串数值，已统一解析）。
  - 概览页新增分组「套餐 / 额度」（小米 MiMo 套餐 + 商汤日日新），并加了兜底分组：以后新增平台即使忘了登记分组，也不会出现「账号存在但看不到卡片」。
- 适配器正式测试脚本 `scripts/verify-adapters.mjs` 新增日日新用例（5h/7d/专属池解析、登录态过期、无积分池、缺凭据），并修复该脚本一个既有缺陷：适配器链路会经 `logger → paths → electron` 把 electron 包打进 ESM，导致 `Dynamic require of "child_process" is not supported` 直接崩，现改用 electron 桩，脚本可正常跑（130 项全通过）。

### 修复
- **阿里云代金券「读不到」时不再静默**：以前 `QueryCashCoupons` 失败被静默忽略，用户只看到金额少了一块却说不出原因。现在：
  - 卡片备注会写明「代金券读取失败：<原因>」或「代金券接口返回 N 张但无可用券（状态：…）」；
  - 运行日志记录返回张数 / 可用张数 / 剩余合计；日志级别切到 debug 还会记录每张券的状态、面值、余额、到期时间（券号只记前 6 位）；
  - 券状态判定放宽：只排除明确的过期/已用/冻结状态，避免平台改状态字面量后整批券被判定为不可用。

### 文档
- README、`docs/使用说明.md`、内置使用说明新增「商汤 日日新」接入步骤与额度口径说明；阿里云章节新增「代金券读不到怎么排查」对照表。

## [1.1.4] - 2026-09-14

### 修复
- **「使用报告」页区块间距**：「消耗构成 / 时段分布」下方那两个独立卡片（这段时间的你、疑似充值记录）与底部说明行原本**没有任何间距**，几个方框贴在一起（`.panel` 只有卡片样式、缺下边距，上一行的间距全靠 `.two-col` 的 `margin-top` 撑着）。
  - 竖向间距统一改为 `margin-bottom: var(--gap)`：顶部汇总卡、两列容器、独立面板、底部说明行一律 16px；
  - 两列容器**内部**的面板排除在外（间距交给 grid 的 gap），避免出现 32px 的双倍间距；
  - 实测：四块之间全部为 16px；「每日使用状况」「平台用量」两页复查无同类问题。

## [1.1.3] - 2026-09-14

### 修复
- **开机自启不生效**：重写 `src/main/autostart.ts`，写入系统启动项后**回读注册表校验**，不再"假装成功"：
  - 统一登录项名称：Electron 在 Windows 上写入用的是 `productName`（API 余额面板）、读回用的是 `name`（api-balance-panel），名字对不上导致状态永远读不正确；现在读写都用同一个名字；
  - 写入路径：先走官方 `app.setLoginItemSettings()`，写完后用 `reg query` 回读 Run 键校验；没写进去就自动用 `reg.exe` 直写当前用户 Run 键兜底（无需管理员权限），失败的真正原因会写进日志；
  - 清掉「任务管理器 → 启动」遗留的禁用标记（`StartupApproved\Run`）：Windows 只要看到这条记录就会拦住启动项，之前一旦被禁用就再也开不起来；
  - 关闭自启时同时删除 Run 键与禁用标记，不留残留项；
  - 启动时校准一次：配置里开着但系统启动项被清理掉（换机 / 被优化软件删掉）会自动补写回去；
  - 开机自启启动时**不再弹出主窗口**，直接常驻托盘后台刷新（`--open-at-login`），且这种启动不会被「窗口隐藏时暂停刷新」停掉，开机后余额照常更新。

### 变更
- **「设置 → 显示 → 主题」与「主题外观」页联动**：
  - 深色 / 浅色 = 直接切换当前主题（即主题外观页里的经典深色 / 经典浅色），两边是同一个设置，点一下**立即生效并保存**，不需要再点「保存设置」；
  - 跟随系统 = 按 Windows 深浅色自动在**经典深色 / 经典浅色**之间切换（系统换色时实时跟着变），并在启动时按系统当前状态选好；
  - 跟随系统时，主题对应的内置背景也按解析后的深浅色自动匹配（以前会去找不存在的 `system.png`）；
  - 在「主题外观」页选了设计师主题后，设置页的主题按钮会同步取消高亮，反之亦然。

### 文档
- 「设置 → 系统 → 开机自启」下方新增**真实状态提示**（已写入 / 未写入 / 被任务管理器禁用 / 开发模式跳过），状态来自注册表回读而不是配置里的期望值；开关点一下立即写入启动项，无需再点「保存设置」。
- README、`docs/使用说明.md`、内置使用说明同步说明上述行为。

## [1.1.2] - 2026-09-10

### 文档
- 更新 GitHub Release（v1.1.0）说明，补充**授权与版权**告知（原创作品、需授权范围、联系邮箱）。
- README 与 `docs/` 中的安装包文件名改为通用表述（不再写死具体版本号），并说明 v1.1.1 / v1.1.2 为授权与文档更新、未单独发布安装包。

## [1.1.1] - 2026-09-10

### 变更（授权模式）
- **授权模式由 MIT 变更为「保留所有权利」的专有许可**：本项目为作者原创作品，著作权归作者所有；
  - 允许：个人学习、研究与自用（非商业目的）；
  - 需事先取得作者书面许可：修改源代码、二次开发、衍生作品、复制再分发、任何商业用途、移除署名；
  - 授权申请：18293592881@163.com。
- `LICENSE` 全文替换为专有许可条款；`package.json` 的 `license` 字段改为 `SEE LICENSE IN LICENSE`、`author` 改为作者署名；README 新增「版权与授权」章节并更新许可徽章。
- 说明：v1.0.x / v1.1.0 等历史版本依当时 MIT 许可发布，其授权不变；自本版本起适用专有条款。

## [1.1.0] - 2026-09-10

### 新增
- **停机期间消耗单独标记**：彻底退出软件一段时间后再打开，期间产生的余额下降（无法按天归属）不再堆到"重新打开的那一天"，而是单独统计：
  - 每日使用状况表格新增「停机期间」列（账号行 + 单位合计行），并在页面顶部给出提示条；
  - 近 30 天日历上给对应日子加 **⏸** 标记，当日弹窗明细显示该日的停机期间消耗；
  - 平台用量页同步新增「停机期间」列，CSV 导出也包含该列；
  - 该部分**不计入每日均值、耗尽预估、建议阈值与每日预算判断**（顺带修掉了"停机后重启误报预算超支"的问题）。
  - 判定口径：与上一条快照间隔超过「刷新间隔 × 3」且至少 30 分钟即视为停机（关窗缩到托盘不算，仍会持续刷新）。
- 托盘 tooltip 仍显示最低余额账号/失败数量。

### 变更
- **取消托盘图标的状态变色**：托盘图标固定为软件主图标，不再在"查询失败或低余额"时染红（告警信息仍可通过 tooltip、卡片与系统通知获知）。

### 文档
- README、`docs/使用说明.md`、内置使用说明同步补充"停机期间消耗"的口径说明。

## [1.0.1] - 2026-09-10

### 变更
- **托盘图标统一为软件主图标**：不再使用单独的托盘小图标，正常态显示软件主图标（`icon.png`）；有查询失败或低余额时，主图标染为红色作为告警（保留原有告警语义）。
- **后台运行时可通过快捷方式唤起主界面**：程序已在后台（窗口隐藏到托盘或最小化）时，再次点击桌面/开始菜单快捷方式或重复启动，会把已有主窗口唤到前台，而不再只是聚焦不可见窗口。
- 单实例约束显式化：全程只允许一个实例，重复启动一律复用已有窗口，不产生多个界面。
- 移除冗余的 `focusMainWindow()`（其行为不完整，已被 `showMainWindow()` 取代）。

### 文档
- 新增本 CHANGELOG。
- README、`docs/使用说明.md`、内置使用说明同步为 **安装包（Setup exe）** 版本，并更新版本号引用为 1.0.1。

## [1.0.0] - 2026-09-10

### 新增（首次发布）
- 余额卡片墙：DeepSeek / 硅基流动 / 小米 MiMo（余额 + 套餐）/ MiniMax / 智谱 / 阿里云百炼 / New API 中转站 / 自定义接口
- 自动定时刷新、低余额提醒、余额耗尽预估、每日预算与告警
- 每日使用状况（日历热力图）、平台用量统计、AI 使用报告与分享卡片、CSV 导出
- 接口监控：自填 Key + 调用地址，按间隔真实调用并统计可用率 / 延迟 / 连续失败
- Key 管理：分类标签、回收站、批量操作、导出 Markdown/JSON/cURL
- 14 套主题 + 内置背景 + 玻璃拟态效果、演示模式、系统托盘常驻、开机自启、备份恢复
- 密钥本机加密（Windows DPAPI），渲染进程不接触明文，不上传任何数据

### 修复
- 概览页「删除账号」失效（IPC 入参对象 / 裸字符串不匹配）
- 重启后账号分类标签与回收站状态丢失（配置加载未保留 `tags` / `deleted_at`）
- 卡片「余额耗尽档位」永远显示正常（百分比口径错误）
- 接口监控展开历史后点「检查」会收起面板（toggle 语义冲突）

### 清理
- 删除已被「接口监控」取代的探测模块死代码（`probe.ts`、`buildAvailability` 及相关类型/设置项）
- 删除无调用者的导出与死 CSS，抽取重复的 CSV 导出逻辑与表单字段定义
- 托盘菜单精简为「显示主面板 / 退出」两项
